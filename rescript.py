RESearch = input()

# Take input from user

# Read in Input from user

# Figure out which operator its using . | or *

# Concatenation operator . would be a.b so anything with ab next to each other

# Or operator would be anything a|b so anything that contains a or b

# Kleene star a* anything that has one or more 

file = open("input.txt")

#if "." in RESearch: 
    # Then get letters infont of and behind Operator and see if anything in file matches
    # If result return amount of matches using if loop?

    #return
    #else

#if "|" in RESearch:
    #else

#if "*" in RESearc:
    #else

# After that search text file for anything contain letters with the corresponding operator 



file.close()

print(RESearch)